#include "main.h"
/**
 * _abs - Write a function that makes a number absolute.
 * @n: Is an int value.
 *
 * Return: The absolute value of n.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
