#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */with proper grammar, but the outcome is a piece of art,
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
