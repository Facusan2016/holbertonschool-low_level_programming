#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char toprint[] = "\"Programming is like building a multilingual puzzle";

	puts(toprint);
	return (0);
}
